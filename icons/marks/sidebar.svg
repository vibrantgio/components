<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" width="24" height="24">
  <!-- Sidebar, the drawing every platform without one of its own gets: a
       rounded pane divided into a narrow leading column and a wide trailing
       one. One picture for the control in every state — showing and hiding a
       sidebar is the control's business to signal, not the mark's, so nothing
       here morphs.

       The figure carries the platform-neutral structure only; the faint list
       lines that stand for a source list are a macOS habit and live in that
       platform's own file.

       MEASURED at 1x, voicememos-window.png, the sidebar toggle standing in
       that window's toolbar band (the capsule x 96-135, the symbol x 106-125,
       y 18-33, in a control 36 px tall — the 24-unit mark box, so one px there
       is one unit here). Read against the control's own fill of 39 and a
       foreground of 233:

         the pane      19.31 by 15.12 units
         the band       1.39 units, all four sides — the leading side reads
                        233 then 115, which is 1.00 + 0.39 of a pixel, and the
                        trailing side 54, 233, 100, which is 0.08 + 1.00 + 0.31
         the seam       1.33 units, standing 4.99 units in from the inner pane
                        with 10.21 after it: one to 2.05
         the corners    a radius near 3 units — column 106 arrives at 0.05,
                        0.60, 0.92 and 1.00 down the four rows below the pane's
                        top edge, which a circle of r 3 gives as 0, 0.67, 0.98
                        and 1.00 and one of r 2 as 0, 0.96, 1.00, 1.00

       Grid: the pane is 19 by 15 on the 24 grid, so its sides sit on the
       set's square keyline 2.5 units in and its top and bottom at 4.5 and
       19.5 — the measured 19.31 drawn at the keyline and the measured 15.12
       at the nearest whole unit. The band is the set's axis-aligned
       measure of 1.5 units, a sixth over the measured 1.39, which is the miss
       the whole set carries; every edge here runs along the grid, so none of
       them takes the diagonal compensation. That gives an inner pane of 16 by
       12. Corner radius 3 outside and 1.5 inside, which keeps the band even
       all the way round; a sharper corner reads as a screen rather than a
       window. Outer contour clockwise, inner counter-clockwise, so the band is
       a hole under the non-zero winding rule.

       The seam stands at 8.75, which is the measured one to 2.05 drawn on the
       quarter unit: 4.99 against 10.21 over an inner pane of 16 puts its
       leading edge at 8.76, leaving a leading column of 4.75 against a
       trailing one of 9.75. Its own contour, so it abuts the frame rather
       than winding against it.

       Where each band lands. The pane's top at 4.5 (m=3) and its foot at 18
       (m=12) are on the 1.5 sub-grid and land a whole device pixel at 16, 20
       and 24 dp alike. The two SIDES do not, and cannot: the keyline's 19
       units is odd against a 24-unit box, so they stand at 2.5 and 20 and
       land a whole pixel at 24 dp only, reaching 0.917 of their best pixel at
       20 dp and 0.667 at 16. The SEAM lands at 24 dp and reaches 0.833 at
       16 dp and 0.708 at 20. The measurement stands over the third size's
       crispness in all three cases, which is the trade the keyline's own
       reading already made for the set.
 -->
  <path fill="#000000" d="M18.5 4.5 C20.157 4.5 21.5 5.843 21.5 7.5 L21.5 16.5 C21.5 18.157 20.157 19.5 18.5 19.5 L5.5 19.5 C3.843 19.5 2.5 18.157 2.5 16.5 L2.5 7.5 C2.5 5.843 3.843 4.5 5.5 4.5 Z M5.5 6 C4.672 6 4 6.672 4 7.5 L4 16.5 C4 17.328 4.672 18 5.5 18 L18.5 18 C19.328 18 20 17.328 20 16.5 L20 7.5 C20 6.672 19.328 6 18.5 6 Z"/>
  <!-- The seam, one band wide, spanning the inner pane from top to
       bottom. -->
  <path fill="#000000" d="M8.75 6 L10.25 6 L10.25 18 L8.75 18 Z"/>
</svg>
